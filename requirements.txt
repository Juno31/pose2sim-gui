# Core pipeline
pose2sim>=0.10

# GUI
pywebview>=5.0

# Pose estimation
rtmlib
onnxruntime              # CPU inference — replace with onnxruntime-gpu for NVIDIA GPU
opencv-contrib-python

# Utilities
numpy>=1.23
scipy>=1.10
toml>=0.10
